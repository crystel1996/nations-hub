import { SxProps } from "@mui/material";

const MainLayoutStyle: Record<string,SxProps> = {
    container: {
        marginTop: '64px',
        height: 'calc(100vh - 64px)',
        width: '100vw',
        overflowY: 'auto',
    }
}

export default MainLayoutStyle;