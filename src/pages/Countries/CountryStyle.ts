import { SxProps } from "@mui/material";

export const CountryStyle: Record<string, SxProps> = {
    content: {
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center'
    }
}