import { HeaderMenuInterface } from "@nations-hub/components/UI/Header/interface";

const MENU: HeaderMenuInterface[] = [
    {
        label: 'Countries',
        link: '/countries'
    }
];

export default MENU;